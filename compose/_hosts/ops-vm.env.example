# ops-vm 인프라 컨테이너 통합 env (caddy / postgres / registry).
# 각 기능 폴더의 .env.example 도 같은 변수를 보여주지만, 운영 정공법은 wrapper 단일 .env.

# caddy
AIRFLOW_DOMAIN=airflow.<your-domain>

# postgres
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# registry
OPS_TAILNET_IP=
