services:
  rover:
    image: registry.internal:5000/rover:arm64
    container_name: rover
    expose:
      - "9000"
    volumes:
      - ./plan.json:/src/plan.json:ro
    command: -planJSONPath /src/plan.json
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    networks:
      - nexus
    restart: unless-stopped

networks:
  nexus:
    external: true
