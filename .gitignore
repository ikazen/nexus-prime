# secrets — 실제 값은 password manager. 암호화본(*.enc.env)은 commit
.env
*.env
!.env.example
!*.env.example
!*.enc.env

# OpenTofu
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
*.tfvars
!*.tfvars.example
# .terraform.lock.hcl 은 commit (provider 버전 lock = 재현성)

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
*.swp
*.swo

# python (omnigent local tools 등)
__pycache__/
*.pyc

# secrets backup
secrets-backup.md

# monitoring — Discord webhook URL 포함, example 만 commit
compose/monitoring/alertmanager.yml
